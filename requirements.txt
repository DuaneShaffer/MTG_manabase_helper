# Runtime — the Python data pipeline (core/ + scripts/build_data.py).
# The web app in docs/ is fully static and needs no Python at runtime.
requests>=2.28        # Scryfall fetch (data build)

# Development
pytest>=7.0
