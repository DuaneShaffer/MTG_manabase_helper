# Runtime — the Python side (core/, mtg_test.py CLI, scripts/build_data.py).
# The web app in docs/ is fully static and needs no Python at runtime.
requests>=2.28        # Scryfall fetch (CLI + data build)

# Development
pytest>=7.0
